/**
 * Every CTA on the site is a mailto link. These may be swapped for scheduling
 * links later — change them here, not at the call sites.
 */

export const EMAIL = "hello@hopperlace.ai";

export const MAIL_HREF = `mailto:${EMAIL}`;
export const CONSULTATION_HREF = `mailto:${EMAIL}?subject=Consultation`;
export const DEMO_HREF = `mailto:${EMAIL}?subject=Benchmark%20platform%20demo`;
